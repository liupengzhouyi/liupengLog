<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.liupeng.liupenglog.dao.LiupengFlogDao">

    <resultMap type="cn.liupeng.liupenglog.entity.LiupengFlog" id="LiupengFlogMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="flogAbstruct" column="flog_abstruct" jdbcType="VARCHAR"/>
        <result property="stratDate" column="strat_date" jdbcType="VARCHAR"/>
        <result property="endDate" column="end_date" jdbcType="VARCHAR"/>
        <result property="isStart" column="is_start" jdbcType="INTEGER"/>
        <result property="isEnd" column="is_end" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="isDelete" column="is_delete" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="LiupengFlogMap">
        select id,
               name,
               flog_abstruct,
               strat_date,
               end_date,
               is_start,
               is_end,
               user_id,
               is_delete
        from flog.liupeng_flog
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="LiupengFlogMap">
        select id,
               name,
               flog_abstruct,
               strat_date,
               end_date,
               is_start,
               is_end,
               user_id,
               is_delete
        from flog.liupeng_flog limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="LiupengFlogMap">
        select id,
               name,
               flog_abstruct,
               strat_date,
               end_date,
               is_start,
               is_end,
               user_id,
               is_delete
        from flog.liupeng_flog
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into flog.liupeng_flog(name, flog_abstruct, strat_date, end_date, is_start, is_end, user_id, is_delete)
        values (#{name}, #{flogAbstruct}, #{stratDate}, #{endDate}, #{isStart}, #{isEnd}, #{userId}, #{isDelete})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into flog.liupeng_flog(name, flog_abstruct, strat_date, end_date, is_start, is_end, user_id, is_delete)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.flogAbstruct}, #{entity.stratDate}, #{entity.endDate}, #{entity.isStart},
            #{entity.isEnd}, #{entity.userId}, #{entity.isDelete})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into flog.liupeng_flog(name, flog_abstruct, strat_date, end_date, is_start, is_end, user_id, is_delete)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.flogAbstruct}, #{entity.stratDate}, #{entity.endDate}, #{entity.isStart},
            #{entity.isEnd}, #{entity.userId}, #{entity.isDelete})
        </foreach>
        on duplicate key update
        name = values(name) , flog_abstruct = values(flog_abstruct) , strat_date = values(strat_date) , end_date =
        values(end_date) , is_start = values(is_start) , is_end = values(is_end) , user_id = values(user_id) , is_delete
        = values(is_delete)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update flog.liupeng_flog
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="flogAbstruct != null and flogAbstruct != ''">
                flog_abstruct = #{flogAbstruct},
            </if>
            <if test="stratDate != null and stratDate != ''">
                strat_date = #{stratDate},
            </if>
            <if test="endDate != null and endDate != ''">
                end_date = #{endDate},
            </if>
            <if test="isStart != null">
                is_start = #{isStart},
            </if>
            <if test="isEnd != null">
                is_end = #{isEnd},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="isDelete != null">
                is_delete = #{isDelete},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from flog.liupeng_flog
        where id = #{id}
    </delete>

</mapper>

